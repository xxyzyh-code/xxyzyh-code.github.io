# .github/workflows/cpanel_deploy.yml
# 專門用於構建 Jekyll 並透過 FTP 部署到 cPanel 伺服器

name: Deploy to cPanel via FTP

on:
  # 當 push 到 main 分支時觸發自動部署
  push:
    branches: ["main"]
  # 也允許手動從 GitHub 介面觸發
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cpanel_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      # 確保使用與您 Jekyll 專案相符的 Ruby 版本 (您之前使用的是 3.2)
      - name: Setup Ruby (Required for Jekyll Build)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true  # 安裝 Gemfile.lock 中的依賴

      - name: Build Jekyll Site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production
      
      - name: 🚀 Deploy built files to cPanel /public_html (via SFTP)
        # 🚨 關鍵修正點：更換為 SFTP 專用 Action
        uses: easingthemes/ssh-deploy@main 
        with:
          # 連接資訊 (使用 GitHub Secrets)
          SSH_HOST: ${{ secrets.FTP_HOST }}
          SSH_USERNAME: ${{ secrets.FTP_USERNAME }}
          SSH_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          # 🚨 SFTP 預設使用 22 號端口，無需特別指定，但如果需要可以加上：
          # PORT: 22 
          
          # 上傳 Jekyll 的輸出目錄
          source: ./_site/
          # 您的網站根目錄
          target: /home/xxyzyh/public_html/
          
          # 清理目標目錄上不再需要的舊文件 (這是關鍵，類似於 clean-slate)
          EXCLUDE: "/.git*, /.DS_Store, /.cpanel.yml"
          
          # 增加 log-level 方便除錯
          ARGS: "-vvv" # 這會增加 SFTP 的輸出日誌
