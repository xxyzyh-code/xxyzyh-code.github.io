title: 你的新博客名称
email: xjxnnd@example.com
description: >-
  这是你的 Minimal Mistakes 博客描述。
  用于记录你的想法、学习和生活。
baseurl: ""
url: "https://xxyzyh-code.github.io"

remote_theme: "mmistakes/minimal-mistakes"

minimal_mistakes_skin: "default"
locale: "zh-CN"
timezone: "Asia/Shanghai"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-archives
  - jekyll-remote-theme
  - jekyll-include-cache

include:
  - _pages
  - _data
  - _headers
  
# 🚀 【關鍵變動】：新增 'api/' 到排除列表，阻止 Jekyll 處理這個資料夾
exclude:
  - vendor/
  - node_modules/
  - Gemfile
  - Gemfile.lock
  - package.json
  - package-lock.json
  - README.md
  - functions/
  - api/ 

author:
  xxyzyh:
    name: "小写博客主人"
    avatar: "/assets/images/avatar.jpg"
    bio: "一个热爱写作和分享的个人博客作者。"
    email: xjxnnd@example.com
    location: "中国"
    links:
      - label: "GitHub"
        url: "https://github.com/xxyzyh-code"
      - label: "Twitter"
        url: "https://twitter.com/yhz926"

defaults:
  # 1. 针对所有文章 (posts) 设置默认封锁
  - scope:
      path: ""
      type: "posts"
    values:
      layout: single
      author_profile: true
      # 🚀 封锁文章的核心：
      robots: "noindex, nofollow" 
      seo:
        noindex: true
        
  # 2. 针对所有页面 (pages) 设置默认封锁 (包括 /about/, /tags/ 等)
  - scope:
      path: ""
      type: "pages"
    values:
      layout: single
      author_profile: true
      # 🚀 封鎖頁面的核心：
      robots: "noindex, nofollow" 
      seo:
        noindex: true

  # 3. 针对所有文件 (包括您的首页 index.md)
  - scope:
      path: ""
    values:
      # 这里可以保留或删除原有的设置
      layout: single
      author_profile: true


search: true   # ⚠️ 請注意，這裡的 `search: false` 已經被後面的 `search: true` 覆蓋。
paginate: 5
paginate_path: "/page:num/"

markdown: kramdown
highlighter: rouge

# ---------------------------------------------
# 站内搜索设置（纯前端，无需插件）
# ---------------------------------------------
search: true
search_full_content: true
